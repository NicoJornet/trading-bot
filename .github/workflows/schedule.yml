name: APEX Weekly Scanner

on:
  schedule:
    # Cron syntax: Minute Heure JourMois Mois JourSemaine
    # 18:00 UTC = 19:00 ou 20:00 France (selon été/hiver)
    # Le '0' final signifie Dimanche
    - cron: '0 18 * * 0'
  
  # Permet de lancer manuellement le script via un bouton (pratique pour tester)
  workflow_dispatch:

jobs:
  run-scanner:
    runs-on: ubuntu-latest

    steps:
    - name: Récupérer le code (Checkout)
      uses: actions/checkout@v3

    - name: Installer Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Installer les dépendances
      run: |
        pip install -r requirements.txt

    - name: 🚀 LANCER LE SCAN APEX
      run: |
        python main.py
