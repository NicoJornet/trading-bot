name: APEX Weekly Scanner

on:
  schedule:
    # Lancement tous les dimanches à 18h00 UTC
    - cron: '0 18 * * 0'
  
  # Bouton pour lancer manuellement
  workflow_dispatch:

jobs:
  run-scanner:
    runs-on: ubuntu-latest

    steps:
    - name: Récupérer le code (Checkout)
      uses: actions/checkout@v3

    - name: Installer Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'  # <--- CORRECTION ICI (3.9 -> 3.10)

    - name: Installer les dépendances
      run: |
        pip install -r requirements.txt

    - name: 🚀 LANCER LE SCAN APEX
      run: |
        python main.py
